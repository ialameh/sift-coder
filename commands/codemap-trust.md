---
description: codemap-trust workflow — see corresponding skills/*/codemap-trust/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:codemap-trust

Direct invocation of the `codemap-trust` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
