---
description: blast-radius workflow — see corresponding skills/*/blast-radius/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob, WebFetch
---

# /siftcoder:blast-radius

Direct invocation of the `blast-radius` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
