---
description: sync workflow — see corresponding skills/*/sync/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:sync

Direct invocation of the `sync` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
