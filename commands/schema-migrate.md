---
description: Salesforce schema migration — diff, plan, deploy, rollback. Different from /migrate (data). See skills/salesforce/schema-migrate/SKILL.md
argument-hint: [diff|plan|deploy|verify] [--target-org <alias>]
allowed-tools: Read, Edit, Bash, Grep, Glob
---

# /siftcoder:schema-migrate

Salesforce metadata schema migration with risk classification (breaking / lossy / safe), pre-deploy data transforms, validation deploy, post-deploy reconciliation, and explicit rollback strategy per change.

See `skills/salesforce/schema-migrate/SKILL.md`.
