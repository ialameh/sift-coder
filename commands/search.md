---
description: Federated search across code + memory + docs. See skills/knowledge/search/SKILL.md
argument-hint: <query> [--code|--memory|--docs]
allowed-tools: Read, Grep, Glob, Bash
---

# /siftcoder:search

Unified search across three sources: code (Grep), memory (mem_search MCP), docs (markdown). RRF-fused, ranked, source-labelled.

`$ARGUMENTS` is the query. Optional flags filter to one source.

See `skills/knowledge/search/SKILL.md`.
