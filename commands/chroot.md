---
description: chroot workflow — see corresponding skills/*/chroot/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:chroot

Direct invocation of the `chroot` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
