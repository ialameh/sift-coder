---
description: codemap workflow — see corresponding skills/*/codemap/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:codemap

Direct invocation of the `codemap` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
