---
description: api workflow — see corresponding skills/*/api/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:api

Direct invocation of the `api` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
