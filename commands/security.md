---
description: security workflow — see corresponding skills/*/security/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob, WebFetch
---

# /siftcoder:security

Direct invocation of the `security` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
