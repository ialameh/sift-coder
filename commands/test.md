---
description: Multi-modal test generation — unit, integration, e2e, property, mutation, coverage, bulk. See skills/coding/test/SKILL.md
argument-hint: [unit|integration|e2e|property|mutation|coverage|bulk] <target>
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:test

Generate tests in the right shape per intent. Pick the mode; behaviour-enumerated; mutation-test sanity-checked.

Different from `/siftcoder:tdd` (which is the discipline).

See `skills/coding/test/SKILL.md`.
