---
description: prompt workflow — see corresponding skills/*/prompt/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:prompt

Direct invocation of the `prompt` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
