---
description: reverse-spec workflow — see corresponding skills/*/reverse-spec/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob, WebFetch
---

# /siftcoder:reverse-spec

Direct invocation of the `reverse-spec` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
