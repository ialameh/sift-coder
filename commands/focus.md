---
description: Narrow attention to a feature/file set (advisory). Different from /scope (which enforces). See skills/workflow/focus/SKILL.md
argument-hint: [set <name> <patterns>|status|clear|extend]
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:focus

Set / clear / status the current focus area. Biases memory retrieval toward this region; does NOT block writes (use `/scope` for that).

See `skills/workflow/focus/SKILL.md`.
