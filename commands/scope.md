---
description: scope workflow — see corresponding skills/*/scope/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:scope

Direct invocation of the `scope` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
