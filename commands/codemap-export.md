---
description: codemap-export workflow — see corresponding skills/*/codemap-export/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:codemap-export

Direct invocation of the `codemap-export` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
