---
description: continue workflow — see corresponding skills/*/continue/SKILL.md
allowed-tools: Read, Edit, Write, Bash, Grep, Glob
---

# /siftcoder:continue

Direct invocation of the `continue` skill. See the skill body for the full workflow contract.

$ARGUMENTS is passed as topic/scope to the skill.
